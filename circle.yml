machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t --tag="citizenlab" --file="/home/ubuntu/synfonyAPI/app/Dockerfile".

test:
  override:


deployment:
  hub:
    branch: master
    commands:
      - docker login -e dev@cityzensciences.fr -u citizenlab -p czsdev2015
      - docker push test